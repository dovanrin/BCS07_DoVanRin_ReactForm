import React from "react";
import ReactDOM from "react-dom/client";

import RenderGlass from "./Render/RenderGlass";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <>
    <RenderGlass />
  </>
);
